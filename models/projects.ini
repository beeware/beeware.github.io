[model]
name = Projects
label = Projects
hidden = yes

[children]
model = project
order_by = -maturity, project_type, name

[fields.title]
label = Title
type = string

[fields.sort_key]
label = Sort order
type = sort_key